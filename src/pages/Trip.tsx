import { IonPage, IonContent } from '@ionic/react';

const Trip: React.FC = () => {
  return (
    <IonPage>
      <IonContent className="ion-padding">
        <h1>Trip Page</h1>
      </IonContent>
    </IonPage>
  );
};

export default Trip;